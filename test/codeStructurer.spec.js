const test = require('tape');
const structurer = require('../extension/lib/code-structurer');

test('<code> structurer', (t) => {
    t.test('create tag structure that Prism needs', (t) => {
  
    });
    t.end();
});
